# 企業資料庫 專案交接清單（2026-09-12）

## 1) 搬移結果
- 來源：`C:\Users\User\Documents\Codex\2026-09-12\企業資料庫`
- 目標：`C:\Users\User\OneDrive\Desktop\CODEX\企業資料庫`
- 本次搬移：完成（完整複製 914 檔／140 資料夾；來源仍保留備份，尚未刪除）
- 總檔案（目的地）：914

## 2) 具名帳號與正式庫搜尋（已接上）
- 帳號：Wayne（admin）、Alex、Dios、Gary
- 預設密碼四人相同，登入後可自行修改
- 材料搜尋：工作台 API `/api/search` → 正式庫 `http://127.0.0.1:8765`
- 搜尋紀錄記名，Wayne 可看全部，其他人看自己的
- 實驗筆記仍在 `recipes.db`，會標 `created_by`

## 2) 核心檔案確認
- `app.py`：OK
- `recipe_server.py`：OK
- `requirements.txt`：OK
- `scripts/start_all.ps1`：OK
- `scripts/start_streamlit.ps1`：OK
- `scripts/stop_all.ps1`：OK
- `web/index.html`：OK
- `README.md`：OK

## 3) 啟動與驗證
- 後端啟動指令（已有）：
  - `Set-Location "C:\Users\User\OneDrive\Desktop\CODEX\企業資料庫"`
  - `./scripts/start_all.ps1 -WithStreamlit`（本機本次嘗試中會在缺少 streamlit 時中止 Streamlit 啟動，但 API 仍可啟動）
- API 健康檢查：
  - `Invoke-WebRequest -Uri "http://127.0.0.1:8088/api/health"`
  - 本機回應：`200`，內容含 `{"ok": true, "model": "qwen2.5:7b-tw", ...}`
- Streamlit 檢查：
  - `Invoke-WebRequest -Uri "http://127.0.0.1:8501/"`
  - 本機目前回應：連線被拒（未啟動 Streamlit）

## 4) Streamlit 未能啟動原因
- 環境目前無 `streamlit` 可執行套件可供 `python` 呼叫
- 既有 `requirements.txt` 含 `streamlit`，但本機無法正常 `pip install`（網路/權限限制）

## 5) 其他機器上完整啟動步驟
1. `cd C:\Users\User\OneDrive\Desktop\CODEX\企業資料庫`
2. `python -m pip install -r requirements.txt`（有權限可用的 Python 環境）
3. `./scripts/start_all.ps1 -WithStreamlit`
4. 確認：
   - `http://127.0.0.1:8088/api/health`（API）
   - `http://127.0.0.1:8088/`（API 文件/測試）
   - `http://127.0.0.1:8501/`（Streamlit）

## 5) 常用維護
- 只停 API：`./scripts/stop_all.ps1`
- 若後續調整：確認 `recipes.db` 權限與路徑權限
